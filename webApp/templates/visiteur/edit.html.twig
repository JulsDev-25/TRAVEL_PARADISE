{% extends 'base.html.twig' %}

{% block title %}{{ button_label == 'Update' ? 'Modifier' : 'Créer' }} un visiteur{% endblock %}

{% block body %}
    <h1>{{ button_label == 'Update' ? 'Modifier' : 'Créer un nouveau' }} visiteur</h1>

    {{ include('visiteur/_form.html.twig', {'button_label': button_label|default('Enregistrer')}) }}

    <a href="{{ path('app_visiteur_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
