{% extends 'base.html.twig' %}

{% block title %}{{ button_label == 'Update' ? 'Modifier' : 'Nouvelle' }} présence{% endblock %}

{% block body %}
    <h1 class="mb-4">{{ button_label == 'Update' ? 'Modifier' : 'Ajouter une' }} présence</h1>

    {{ include('visit_visiteur/_form.html.twig', {'button_label': button_label|default('Enregistrer')}) }}

    <a href="{{ path('app_visit_visiteur_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
