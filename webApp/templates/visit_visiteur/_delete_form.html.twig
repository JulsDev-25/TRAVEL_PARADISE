<form method="post" action="{{ path('app_visit_visiteur_delete', {'id': visit_visiteur.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visit_visiteur.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
