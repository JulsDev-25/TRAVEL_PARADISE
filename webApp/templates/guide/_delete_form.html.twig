<form method="post" action="{{ path('app_guide_delete', {'id': guide.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce guide ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
